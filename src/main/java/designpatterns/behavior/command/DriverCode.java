package designpatterns.behavior.command;
/*
The command pattern encapsulates a request as an object,
 thereby letting us parameterize other objects with different requests,
 queue or log requests, and support undoable operations.

Concept revolves around Client, Recievers

In command pattern receivers bind with actions

Gives execute method that perform some action on reciever object

 */
public class DriverCode {
}
